# Application properties for FinPulse — The Lending Echosystem
spring.application.name=FinPulse

# Server configuration
server.port=1992

# Database configuration (AWS RDS)
spring.datasource.url=jdbc:mysql://${DB_HOST:finpulse.cnoa00g8aosy.ap-south-1.rds.amazonaws.com}:${DB_PORT:3306}/${DB_NAME:mm_hdfc_db}?useSSL=false&allowPublicKeyRetrieval=true&serverTimezone=Asia/Kolkata
spring.datasource.username=${DB_USERNAME:admin}
spring.datasource.password=${DB_PASSWORD:FinPulse2024Secure}
spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver

# JPA configuration
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.MySQL8Dialect
spring.jpa.properties.hibernate.format_sql=true

# JWT configuration
jwt.secret=${JWT_SECRET:MoneyMoment@LendingPlatform#2026$SecureKey}
jwt.expiration.ms=86400000

# Cloudinary Storage
cloudinary.cloud-name=${CLOUDINARY_CLOUD_NAME:dfxkub9xm}
cloudinary.api-key=${CLOUDINARY_API_KEY:389421956124284}
cloudinary.api-secret=${CLOUDINARY_API_SECRET:s_Bp40p82C0ScRBAZUQmMFn1piI}

# Multipart file upload
spring.servlet.multipart.max-file-size=10MB
spring.servlet.multipart.max-request-size=12MB
